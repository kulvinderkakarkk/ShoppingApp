const reviews = {
    Query: {
        async getReviews(product_id, username) {
            return []
        }
    },
    Mutation: {
        async addReview(reviewDetails) {
            return {}
        }
    }
}

export default reviews