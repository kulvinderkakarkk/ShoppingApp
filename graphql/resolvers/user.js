
const user = {
    Mutation : {
        async register(userDetails) {
            return {}
        },
        async login(username, password) {
            return {}
        }
    }
}

export default user;